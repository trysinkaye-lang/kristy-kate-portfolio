import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft } from "lucide-react";
import { projects } from "@/data/projects";

export function generateStaticParams(){return projects.map(p=>({slug:p.slug}));}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();return <main id="main-content" className="mx-auto max-w-6xl px-6 pb-24 pt-32 lg:px-8"><Link className="social-link" href="/#projects"><ArrowLeft size={16}/> Back to projects</Link><div className="mt-8"><p className="eyebrow">Case study · {p.status}</p><h1 className="mt-4 max-w-5xl text-4xl font-semibold tracking-tight text-white sm:text-6xl">{p.title}</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-300">{p.overview}</p></div><img className="mt-10 w-full rounded-[2rem] border border-white/10 bg-white/[.03]" src={p.image} alt={`${p.title} placeholder visual`}/><div className="case-grid"><Case title="The problem"><p>{p.problem}</p></Case><Case title="The solution"><p>{p.solution}</p></Case><Case title="My role"><p>{p.role}</p></Case><Case title="Technology stack"><div className="flex flex-wrap gap-2">{p.technologies.map(t=><span className="tech-chip" key={t}>{t}</span>)}</div></Case><Case title="Key features"><ul>{p.features.map(x=><li key={x}>{x}</li>)}</ul></Case><Case title="Challenges"><ul>{p.challenges.map(x=><li key={x}>{x}</li>)}</ul></Case><Case title="UI/UX process"><p>Problem → Research → Wireframe → Interface → Development → Testing → Improvement</p></Case><Case title="System architecture"><p>Add a verified architecture diagram here. Recommended: client/interface → application/API layer → database/storage → reporting/sync services.</p></Case><Case title="Lessons learned"><ul>{p.lessons.map(x=><li key={x}>{x}</li>)}</ul></Case><Case title="Screenshots"><p>Replace the current placeholder artwork with real screenshots, annotated flows, or before/after interface examples.</p></Case></div></main>}
function Case({title,children}:{title:string;children:React.ReactNode}){return <section className="case-card"><p className="eyebrow">{title}</p><div className="mt-4 leading-7 text-slate-300 [&_li]:mb-2 [&_li]:ml-5 [&_li]:list-disc">{children}</div></section>}
